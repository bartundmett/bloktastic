{
  "name": "bloktastic",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.5",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter website dev",
    "dev:cli": "pnpm --filter ./packages/cli dev",
    "build:cli": "pnpm --filter ./packages/cli build",
    "bloktastic": "node packages/cli/dist/index.js",
    "lint": "pnpm -r lint",
    "check": "pnpm -r check",
    "registry:build": "node scripts/build-registry.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
